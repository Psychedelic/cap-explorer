import { styled } from '@stitched';

const Identicon = styled('span', {
  background: 'linear-gradient(180deg, #421A95 0%, #A49DEA 100%)',
  width: '28px',
  height: '28px',
  display: 'inline-block',
  borderRadius: '100%',
  marginRight: '10px',
});

export default Identicon;
